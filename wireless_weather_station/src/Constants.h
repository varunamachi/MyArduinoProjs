#pragma once

#include <Arduino.h>

namespace Constants {
    const String SSID( "" );
    const String KEY( "" );
    const char * SERVER_IP   = "192.168.0.150";
    const char * SERVER_PORT = "8181";
}
